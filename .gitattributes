# Normalize to LF in the repository; Git checks out CRLF on Windows where it matters.
* text=auto eol=lf

# These are executed by Windows hosts that want CRLF.
*.ps1 text eol=crlf
*.vbs text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf
